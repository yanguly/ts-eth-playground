# Intentionally minimal test environment.
# Keeps runtime env clean so tests control all variables via vm.setEnv.

# Leave token/impl unset to ensure script branches behave deterministically in tests.
# TOKEN_ADDRESS=
# IMPL_V2=

